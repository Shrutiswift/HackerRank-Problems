#include <stdio.h>

int main()
{
    int first, second, add, sub, mult, mod, div;
    printf("Enter two integers\n");
    scanf("%d%d",&first,&second);
    add=first+second;
    sub=first+second;
    mult=first+second;
    div=first/second;
    mod=first%second;
    printf("Sum = %d\n",add);
    
    
    return 0;
}